{
  "name": "opencode-companion-cc",
  "version": "2.0.4",
  "private": true,
  "type": "module",
  "description": "Maintained, hardened OpenCode delegation + review plugin for Claude Code.",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=18.18.0"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.0.0"
  }
}
